# .air.toml (Minimal for Gin)
root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/main ./cmd/server"
bin = "./tmp/main"
delay = 1000
exclude_dir = ["tmp", "vendor", "logs", ".git"]

[log]
time = false

[misc]
clean_on_exit = true
